name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for npm OIDC trusted publishing

    steps:
      - uses: actions/checkout@v6
        with:
          # Provenance signing needs a populated git ref. On the `release`
          # event newer checkout versions detach HEAD without a ref, which
          # drops SourceRepositoryRef from the sigstore cert and causes npm
          # to reject the publish with a 422 (npm/cli#8699, checkout#2280).
          ref: ${{ github.event.release.tag_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install npm 11 (required for OIDC trusted publishing)
        run: npm install -g npm@11.5.1

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # ── Preferred: npm Trusted Publishing (OIDC, no long-lived token) ──────────
      # Provenance attestation is generated automatically.
      # Requires one-time setup on npmjs.com before the first automated publish:
      #   npmjs.com → @dooph-software/mesh-gradients → Settings → Trusted publishers
      #   → Add publisher → GitHub Actions
      #   → Org: dooph-software  Repo: mesh-gradients  Workflow: release-package.yml
      # The package must already exist on npm before trusted publishing can be configured.
      # For the very first publish, use the TOKEN FALLBACK below, then switch here.
      - name: Publish to npm (OIDC)
        run: npm publish --access public
      # No NODE_AUTH_TOKEN needed — npm CLI detects the OIDC environment automatically.
